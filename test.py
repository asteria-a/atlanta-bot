"""import json

with open("atlanta.json", "r") as file:
    data = json.load(file)

    data["default_role"] = 123

with open("atlanta.json", "w") as file:
    file.write(json.dumps(data, indent=2))"""
